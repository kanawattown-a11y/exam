# تطبيق نتائج الامتحانات - جبل باشان

تطبيق ويب حديث لعرض نتائج الامتحانات مع لوحة تحكم كاملة للأدمن.

## 🚀 التشغيل السريع

```bash
# تثبيت المكتبات
npm install

# تشغيل خادم التطوير
npm run dev
```

ثم افتح [http://localhost:3000](http://localhost:3000) في المتصفح.

## 📁 هيكل المشروع

```
src/
├── app/
│   ├── page.tsx              # الصفحة الرئيسية (المؤقت/البحث)
│   ├── layout.tsx            # التخطيط الرئيسي
│   ├── globals.css           # الأنماط العامة
│   └── admin/
│       ├── page.tsx          # لوحة التحكم
│       ├── layout.tsx        # تخطيط الأدمن
│       ├── login/page.tsx    # تسجيل الدخول
│       ├── settings/page.tsx # الإعدادات
│       ├── students/page.tsx # إدارة الطلاب
│       ├── results/page.tsx  # إدارة النتائج
│       ├── sections/page.tsx # إدارة الأقسام
│       ├── subjects/page.tsx # إدارة المواد
│       └── upload/page.tsx   # رفع الملفات
├── components/
│   ├── countdown/            # مكون المؤقت
│   ├── search/               # مكون البحث
│   └── results/              # مكون النتائج
├── lib/
│   ├── db.ts                 # قاعدة البيانات (localStorage)
│   ├── auth.ts               # نظام المصادقة
│   ├── utils.ts              # دوال مساعدة
│   └── excel-parser.ts       # قراءة Excel/CSV
└── types/
    └── index.ts              # أنواع TypeScript
```

## 🔐 بيانات تسجيل الدخول الافتراضية

- **اسم المستخدم**: `admin`
- **كلمة المرور**: `admin123`

## ✨ الميزات

### للطلاب:
- ✅ مؤقت عد تنازلي بتصميم حديث
- ✅ البحث برقم الاكتتاب
- ✅ عرض النتائج الكاملة مع كل المواد
- ✅ عرض النسبة المئوية والتقدير
- ✅ تصدير النتيجة كصورة عالية الدقة

### للأدمن:
- ✅ لوحة تحكم مع إحصائيات
- ✅ إدارة إعدادات المؤقت
- ✅ إضافة/تعديل/حذف الطلاب
- ✅ إضافة/تعديل النتائج
- ✅ إدارة الأقسام والمواد
- ✅ رفع ملفات Excel/CSV

## 🎨 الهوية البصرية

- **اللون الرئيسي**: `#4788c8`
- **التصميم**: Dark Mode + Glassmorphism
- **الخط**: Cairo (عربي)
- **الاتجاه**: RTL بالكامل

## 📦 التقنيات المستخدمة

- **Next.js 14** - إطار العمل
- **TypeScript** - اللغة
- **Tailwind CSS** - التصميم
- **LocalStorage** - قاعدة البيانات (قابلة للتغيير لـ Turso)
- **html-to-image** - تصدير الصور
- **xlsx / papaparse** - قراءة Excel/CSV

## 🌐 النشر على Cloudflare Pages

1. ادفع الكود إلى GitHub
2. اذهب إلى [Cloudflare Pages](https://pages.cloudflare.com)
3. ربط المستودع
4. إعدادات البناء:
   - **Build command**: `npm run build`
   - **Build output directory**: `out`
5. لتفعيل التصدير الثابت، أضف في `next.config.js`:
   ```js
   output: 'export',
   trailingSlash: true,
   ```

## 📝 ملاحظات

- قاعدة البيانات حالياً تستخدم localStorage (للتطوير)
- لإنتاج حقيقي، استبدل `src/lib/db.ts` بـ Turso أو أي قاعدة بيانات
- التطبيق جاهز للاستخدام فوراً مع بيانات تجريبية
